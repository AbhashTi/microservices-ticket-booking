apiVersion: v1
kind: Service
metadata:
  name: match-frontend
  namespace: movie-app
spec:
  selector:
    app: match-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
