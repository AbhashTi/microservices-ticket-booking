# server:
#   port: 8086

# spring:
#   application:
#     name: match-service

  datasource:
    url: jdbc:postgresql://localhost:5434/match_db
    username: match_user
    password: match_pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

# springdoc:
#   swagger-ui:
#     path: /swagger-ui.html
#     operationsSorter: method
#     tagsSorter: alpha

# management:
#   endpoints:
#     web:
#       exposure:
#         include: health,info
# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
#   instance:
#     prefer-ip-address: true
